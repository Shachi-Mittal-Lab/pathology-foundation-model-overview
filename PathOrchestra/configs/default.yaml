hf_token: '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' # lisa's HF
data_root: 'E:\[PROJ]_DCIS\DATA\BRACS\test\training_patches'
checkpoint_path: ''     # currently no checkpoint for decoder

num_classes: 5  
accumulation_steps: 3        
freeze_encoder: true                  # set false for full fine-tuning
batch_size: 4
epochs: 100
lr: 0.0001
target_pixels_per_class: 200000000

plot_freq: 1                          # Update plot every 5 epochs
checkpoint_freq: 1                    # Save every n epochs